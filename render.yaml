services:
  - type: web
    name: astrology-api
    runtime: python
    rootDir: astrology-api
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
    autoDeploy: true
